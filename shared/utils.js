const ERROR_CONSTANTS = {
  MISSING_PARAM: 'Missing a parameter!',
  INVALID_FUNCTION: 'Invalid function!',
}

const MESSAGE_QUEUES = {
  EXECUTION_QUEUE: 'execution',
  EXECUTION_RESPONSE_QUEUE: 'execution-response',
}

module.exports = {
  ERROR_CONSTANTS,
  MESSAGE_QUEUES,
}
